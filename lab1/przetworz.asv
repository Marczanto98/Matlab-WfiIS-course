function przetworz(v, przel)
maks = max(v); 
minim = min(v);
parz = numel(find(mod(v,2)==0)); 
niep = numel(find(mod(v,2)~=0));
niezer = numel(find(v));
if przel == 1
    modified_v = v(1:end-1);
    moj_sin = sin(v)
end


maks
minim
parz
niep
niezer
moj_sin